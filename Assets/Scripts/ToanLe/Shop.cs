using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Shop : MonoBehaviour
{
    //public GameObject ShopPrefab;
    //float scrollPos = 0;
    //float[] pos;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //pos = new float[transform.childCount];
        //float distance = 1f / (pos.Length - 1);
        //for (int i = 0; i < pos.Length; i++)
        //{
        //    pos[i] = distance * i;
        //}
        //if(Input.GetMouseButton(0)) //Test
        //{
        //    scrollPos = ShopPrefab.GetComponent<Scrollbar>().value;
        //} 
        //else 
        //{
        //    for (int i = 0;i < pos.Length;i++)
        //    {
        //        if (scrollPos < pos[i] + (distance / 2) && scrollPos > pos[i] - (distance / 2)) 
        //        { 
        //            ShopPrefab.GetComponent<Scrollbar>().value = Mathf.Lerp(ShopPrefab.GetComponent<Scrollbar>().value, pos[i], 0.1f);
        //        }
        //    }
        //}
    }
}
